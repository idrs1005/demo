<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
    <% @post.each do |post| %>
      <%= post.title %>
    <% end %>
</p>

<p>
  <strong>Content:</strong>
  <% @post.each do |post| %>
      <%= post.content %>
  <% end %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
